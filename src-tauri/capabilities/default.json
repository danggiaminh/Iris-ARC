{
  "identifier": "default",
  "description": "Capability for the main window — deny by default, explicit allows only",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}
